{
  "name": "playwright-test-analytics-neon",
  "version": "1.0.0",
  "description": "Playwright test reporting and analytics dashboard",
  "private": true,
  "workspaces": [
    "packages/reporter",
    "packages/shared",
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "install-all": "npm install --include=dev",
    "dev": "npm run dev -w packages/backend & npm run dev -w packages/frontend",
    "build": "npm run build -w packages/shared && npm run build -w packages/backend && npm run build -w packages/frontend",
    "start": "cd packages/backend && npm run start"
  }
}
